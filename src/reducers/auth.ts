import {Reducer, Action} from "@ngrx/store";

const auth: Reducer<any> = (state, action: Action) => {
  switch (action.type) {
    case "value":
      // code...
      break;

    default:
      // code...
      break;
  }
};
